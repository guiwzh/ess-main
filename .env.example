# 环境变量模板（复制为 .env.development 或 .env.production 使用）

# 开发环境使用 /api（相对路径），让请求走 Vite 代理 → mock 插件可拦截
# 生产环境使用完整地址（如 https://ess.example.com/api）
VITE_API_BASE_URL=
VITE_SUB_OPERATION_URL=
VITE_SUB_ANALYSIS_URL=
